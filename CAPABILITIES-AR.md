# Qoyod MCP (غير رسمي) — المرجع الكامل: ماذا يقدر يسوي، وماذا لا يقدر

الإصدار 2.0.0. كل المعرّفات والأسماء والمبالغ في الأمثلة **افتراضية للتوضيح فقط**. هذا مشروع غير رسمي وغير تابع لقيود.

## 1) الأدوات في ثلاث مجموعات

كل مورد في قيود له حتى ثلاث أدوات، حتى تقدر تسمح أو تمنع مجموعة كاملة من إعدادات تطبيقك:

| المجموعة | أسماء الأدوات | ماذا تفعل |
|---|---|---|
| القراءة | `qoyod_read_*` (22 أداة) | عرض القوائم والتفاصيل وروابط PDF وحالة الاتصال وطلب GET مباشر. لا تغيّر أي شيء |
| الكتابة | `qoyod_write_*` (20 أداة) | إنشاء وتعديل وتخصيص وتسويات وتحويلات المخزون وطلب POST/PUT/PATCH مباشر. تغيّر دفاترك الحقيقية |
| الحذف | `qoyod_delete_*` (7 أدوات) | حذف فواتير المشتريات والفواتير المبسطة وإشعارات المدين وفواتير المبيعات وإشعارات الدائن والسندات، وطلب DELETE مباشر |
| الإعدادات | `qoyod_settings` | الشركة الرئيسية، محفوظة على جهازك فقط |

## 2) التغطية

واجهة قيود العامة فيها **76 طلب** على **19 مورد**، وكلها مغطاة، إضافة إلى ثلاث قراءات تعمل فعلياً لكنها غير موثقة: قائمة الضرائب، ورابط PDF لفاتورة المشتريات، ورابط PDF لإشعار الدائن.

| المورد | القراءة (`qoyod_read_…`) | الكتابة (`qoyod_write_…`) | الحذف (`qoyod_delete_…`) |
|---|---|---|---|
| الحسابات `accounts` | list, get | create | — |
| المنتجات `products` | list, get | create, update | — |
| المستودعات `inventories` | list, get | create, update, adjust, transfer | — |
| تصنيفات المنتجات `product_categories` | list, get | create, update | — |
| وحدات القياس `product_units` | list, get | create | — |
| الضرائب `taxes` | list | — | — |
| الموردون `vendors` | list, get | create, update | — |
| أوامر الشراء `purchase_orders` | list, get | create | — |
| فواتير المشتريات `bills` | list, get, pdf | create, allocate | نعم |
| مدفوعات فواتير المشتريات `bill_payments` | list, get | create | — |
| الفواتير المبسطة `simple_bills` | list, get | create, update, allocate | نعم |
| مدفوعات الفواتير المبسطة `simple_bill_payments` | list, get | create | — |
| إشعارات المدين `debit_notes` | list, get | create | نعم |
| العملاء `customers` | list, get | create, update | — |
| عروض الأسعار `quotes` | list, get | create | — |
| فواتير المبيعات `invoices` | list, get, pdf | create, allocate | نعم |
| مدفوعات فواتير المبيعات `invoice_payments` | list, get | create | — |
| إشعارات الدائن `credit_notes` | list, get, pdf | create | نعم |
| السندات `receipts` | list, get | create, allocate | نعم |
| القيود اليومية `journal_entries` | list, get | create | — |

## 3) أكثر من شركة

كل شركة في قيود لها مفتاح API خاص بها (`QOYOD_API_KEY_1` مع `QOYOD_COMPANY_1_NAME`، و `QOYOD_API_KEY_2` مع `QOYOD_COMPANY_2_NAME`، وهكذا).

- **إذا ذكرت اسم الشركة** تُستخدم مباشرة، بدون أي سؤال.
- **إذا لم تذكرها:**
  - **القراءة** تستخدم "الشركة الرئيسية للقراءة"، والإجابة تذكر اسم الشركة.
  - **التعديل والحذف** يستخدمان "الشركة الرئيسية للتعديل"، ولا يُرسل شيء قبل أن يخبرك المساعد باسم الشركة.
  - إذا لم تحدد شركة رئيسية تظهر لك **قائمة لتختار منها** (نافذة منبثقة في التطبيقات التي تدعمها، أو سؤال في المحادثة)، ويمكنك حفظ اختيارك كشركة رئيسية.
- تغيير الشركة الرئيسية في أي وقت: اطلب مثلاً "اجعل شركتي الشركة الرئيسية للقراءة".
- الوضع الصارم `QOYOD_CONFIRM_WRITES=always` يعرض القائمة قبل كل تعديل حتى لو ذكرت الشركة.
- أرقام السجلات تخص شركة واحدة: لا تُستخدم أرقام شركة مع شركة أخرى.

## 4) مفاتيح التحكم

| المتغير | القيمة الافتراضية | المعنى |
|---|---|---|
| `QOYOD_READ_ONLY` | `false` | يزيل كل أدوات الكتابة والحذف |
| `QOYOD_ALLOW_WRITES` | `all` | أدوات الكتابة: `all` أو `none` أو قائمة مجموعات |
| `QOYOD_ALLOW_DELETES` | `all` | أدوات الحذف: `all` أو `none` أو قائمة مجموعات |
| `QOYOD_BLOCK_SALES_WRITES` | `false` | يزيل أدوات الكتابة والحذف لسجلات المبيعات (المرتبطة بالفوترة الإلكترونية) |
| `QOYOD_TOOLSETS` | `all` | الأدوات الموجودة أصلاً: `sales`, `purchases`, `inventory`, `accounting` |
| `QOYOD_CONFIRM_WRITES` | `when_missing` | `always` = قائمة الاختيار قبل كل تعديل |

المجموعات: **المبيعات** (العملاء، عروض الأسعار، فواتير المبيعات، مدفوعاتها، إشعارات الدائن، السندات)، **المشتريات** (الموردون، أوامر الشراء، فواتير المشتريات، مدفوعاتها، الفواتير المبسطة، مدفوعاتها، إشعارات المدين)، **المخزون** (المنتجات، التصنيفات، الوحدات، المستودعات)، **المحاسبة** (الحسابات، القيود، الضرائب).

## 5) القوائم والفلترة

- كل قائمة ترجع صفحة واحدة مع ملخص `_page` يوضح إذا كان هناك صفحات أخرى. لا تُجمع المبالغ من صفحة واحدة.
- `per_page` و `page` و `sort` (مثل `"issue_date desc"`) و `fetch_all` (حتى 1000 سجل) و `fields` (إبقاء حقول محددة فقط).
- الفلترة `q` (Ransack) مثل `{"issue_date_gteq":"2026-01-01","status_eq":"Approved"}`. المسندات: `_eq _not_eq _cont _start _end _gt _gteq _lt _lteq _in _null`.
- `name_cont` يعمل على العملاء والموردين (ابحث قبل إنشاء جهة جديدة لتجنب التكرار). المنتجات والحسابات لا تُفلتر بالاسم: استخدم `sku_cont` أو `code_start`.
- السندات وقوائم المدفوعات تقبل `kind_eq` بالقيمة `paid` (صرف) أو `received` (قبض).
- فلتر نوع الحساب `type_*` يُطبّق محلياً لأن قيود لا يرجع نتائج له. نوع الحساب في القائمة هو الفئة الأم فقط (Asset, Liability, Equity, Revenue, Expense).
- بعض القوائم يرجعها قيود كاملة بدون ترقيم (الحسابات، المستودعات، الوحدات، التصنيفات، الموردون، مدفوعات الفواتير المبسطة، القيود اليومية)، والخادم يقسمها صفحات محلياً. القيود اليومية ثقيلة: فلترها دائماً بالتاريخ.
- قوائم مدفوعات فواتير المشتريات والمبسطة والمبيعات هي عرض لسجلات السندات نفسها: لا تجمعها مع مجاميع السندات.

## 6) أمثلة كتابة (افتراضية)

- **فاتورة مشتريات مسودة:** `qoyod_write_bills` بالإجراء `create` و `data={"contact_id":<vendor_id>,"status":"Draft","issue_date":"2026-01-15","due_date":"2026-01-31","inventory_id":<id>,"line_items":[{"product_id":<id>,"quantity":1,"unit_price":1000,"tax_percent":15}]}`.
- **فاتورة مبسطة:** `qoyod_write_simple_bills` بالإجراء `create` و `data={"contact_id":<vendor_id>,"status":"Draft","issue_date":"2026-01-15","inventory_id":<id>,"simple_bill_items_attributes":[{"expense_category_id":<expense_account_id>,"total_amount":300,"tax_id":1}]}`.
- **جرد مخزون:** `qoyod_write_inventories` بالإجراء `adjust`، والكمية `actual_quantity` هي الرصيد الجديد الكلي وليست الفرق.
- **تخصيص سند:** `qoyod_write_receipts` بالإجراء `allocate` و `id=<receipt_id>` و `data={"allocatee_type":"Bill","allocatee_id":<bill_id>,"amount":600}` (تخصيص واحد في كل طلب).
- **إشعار دائن:** بنوده تستخدم `discount_percent` بدلاً من `discount` و `discount_type`.
- **الضرائب:** `tax_id` هو 1 (15%) أو 2 (0%) أو 3 (معفى) أو رقم ضريبة من `qoyod_read_taxes`.

إذا فشلت عملية كتابة مع `may_already_be_saved` فلا تُعد إرسالها: تحقق في قيود أولاً، لأن السجل ربما حُفظ.

## 7) ما لا يوفره API قيود أصلاً

- **لا تعديل بعد الإنشاء** لفواتير المبيعات والمشتريات وأوامر الشراء وعروض الأسعار وإشعارات الدائن والمدين والسندات والمدفوعات والقيود والحسابات والوحدات (الاستثناء: الفواتير المبسطة والمنتجات والتصنيفات والجهات والمستودعات).
- **لا اعتماد للمسودات** عبر API: تُعتمد من داخل قيود.
- **لا حذف** للعملاء والموردين والمنتجات والتصنيفات والحسابات والوحدات والمستودعات وأوامر الشراء وعروض الأسعار والقيود والمدفوعات وتسويات المخزون.
- **لا قراءة** لتسويات وتحويلات المخزون بعد إنشائها.
- **لا تقارير محاسبية رسمية** (ميزان المراجعة، قائمة الدخل، الميزانية، الإقرار الضريبي، أعمار الذمم، كشف الحساب). يمكن للمساعد تقريب بعضها بالتجميع لكنها ليست تقارير قيود المعتمدة.
- **لا إرسال للفوترة الإلكترونية (زاتكا)** ولا حالة قبول أو رفض. المتاح قراءة `qrcode_string` و `zatca_details` فقط.
- لا إرسال بالبريد أو واتساب، لا تحويل عرض سعر إلى فاتورة، لا webhooks، لا بيئة تجريبية: كل شيء على بياناتك الحقيقية.

## 8) أشياء تسويها بنفسك داخل قيود

1. توليد مفتاح API لكل شركة (الإعدادات ← الإعدادات العامة ← مفتاح API ← حفظ).
2. اعتماد المسودات.
3. حذف السجلات التي لا يسمح API بحذفها.
4. إنشاء الضرائب المخصصة والمشاريع ومراكز التكلفة والحقول المخصصة وطرق الدفع والعملات والمستخدمين.
5. تفعيل إعداد "خصم إجمالي" (Total CD Discount) إن احتجت خصماً على مستوى المستند.
6. إرسال الفواتير للعملاء وتقارير زاتكا والتقارير المالية المعتمدة.

## 9) تنبيه

أنت مسؤول عن أي تعديل يُجرى على دفاترك. راجع المسودات قبل اعتمادها. هذا البرنامج لا يُعد استشارة ضريبية أو محاسبية أو استشارة في الامتثال لزاتكا.
